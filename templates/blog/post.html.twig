{% extends 'base.html.twig' %}

{% block title %}{{ post.title }}!{% endblock %}

{% block body %}
        <p>ID {{ id }}</p>
        <h3>{{ post.title|upper }}</h3>
        <p>{{ post.date|date('H:i:s') }}</p>
        <p>{{ 567|price}}</p>

{% endblock %}